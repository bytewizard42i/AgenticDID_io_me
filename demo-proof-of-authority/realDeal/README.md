# realDeal, coming next

This side runs the same `shared/scenario.js` against a live Midnight localnet
(node + proof server + indexer) with real ZK proof generation.

Wiring steps are documented in [`../docs/DEMOLAND_VS_REALDEAL.md`](../docs/DEMOLAND_VS_REALDEAL.md)
(§ realDeal wiring). Until then, `npm run demoland` gives the full storyline
against the real compiled circuits with zero setup.
